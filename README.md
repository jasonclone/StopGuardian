# StopGuardian
Deep Reinforcement Learning–Based Adaptive Traffic Signal Optimization  
Using SUMO and a DQN agent.

## Overview
StopGuardian implements a full RL pipeline for traffic signal control at a single urban intersection.  
The project includes:

- A fixed-time baseline controller
- A DQN agent for adaptive signal control 
- A custom SUMO-based Gym environment for the DQN agent to understand the traffic scenario
- A random-policy rollout for state-space exploration
- An Exploratory Data Analysis (EDA) pipeline
- Training, evaluation, and visualization tools 

All simulations use the SUMO traffic simulator.

---
*** Important commands

** Build and run the Docker container (not recommended for testing purposes). 
Make sure container is running via Docker Desktop. This will build the Docker image with the tag "stopguardian." 
<...\StopGuardian> docker build -t stopguardian .
<...\StopGuardian> docker run --rm -it stopguardian

~~ ** generate sumo network
** NOTE: List build time input files in command, not runtime (like routes.rou.xml)
<...\StopGuardian\simulation\sumo> netconvert -n intersection.nod.xml -e intersection.edg.xml -o intersection.net.xml
~~
** intersection.net will now be automatically generated by using sumogui to create intersection geometry, as manually coding geometry is slower and more bug prone.

** run sumo gui manually
<...\StopGuardian> sumo-gui -c simulation.sumocfg

** Get and compare results of fixed time baseline and trained rl agent:

1) run train_dqn.py (train the agent. This will take a while)
<...\StopGuardian> python 

2) run fixed_time_metrics.py and evaluate_rl.py (so their output csvs can be generated)
<...\StopGuardian> python python baselines/b1.py
<...\StopGuardian> python

3) run compare_rl_baseline.py (generated txt file with comparison details)
<...\StopGuardian> python 


** Exploratory Data Analysis of the results of the fixed time baseline, random rollout, and trained rl agent
<...\StopGuardian> python 